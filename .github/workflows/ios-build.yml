name: iOS Build

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  build:
    runs-on: macos-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.24.0'
          cache: true
      
      - name: Get dependencies
        run: flutter pub get
        working-directory: ./aqary_app
      
      - name: Run tests
        run: flutter test
        working-directory: ./aqary_app
      
      - name: Build iOS app
        run: flutter build ios --release --no-codesign
        working-directory: ./aqary_app
      
      - name: Upload iOS build
        uses: actions/upload-artifact@v4
        with:
          name: ios-build
          path: aqary_app/build/ios/iphoneos/Runner.app
